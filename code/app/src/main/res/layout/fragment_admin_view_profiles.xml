<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:orientation="vertical">


    <ImageButton
        android:id="@+id/back_button"
        android:layout_width="40dp"
        android:layout_height="40dp"
        android:layout_marginStart="15dp"
        android:background="@drawable/back_button_background"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        android:layout_marginTop="35dp"
        android:src="@drawable/left_arrow"
        android:contentDescription="Back" />

    <TextView
        android:id="@+id/allProfilesTitle"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="20dp"
        android:text="All Profiles"
        android:textColor="@color/dark_brown"
        android:textSize="34sp"
        app:layout_constraintTop_toBottomOf="@id/back_button"
        app:layout_constraintStart_toStartOf="parent"
        android:layout_margin="10dp"
        android:fontFamily="@font/space_mono_bold"/>

    <SearchView
        android:id="@+id/admin_profile_search"
        android:layout_width="330dp"
        android:layout_height="37dp"
        android:layout_marginStart="16dp"
        android:layout_marginTop="10dp"
        android:background="@drawable/search_bar"
        android:closeIcon="@null"
        android:focusable="true"
        android:iconifiedByDefault="false"
        android:queryBackground="@null"
        android:queryHint="Search"
        android:searchIcon="@null"
        android:submitBackground="@null"
        android:textColor="@color/dark_brown"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toBottomOf="@id/allProfilesTitle"/>

    <ImageButton
        android:id="@+id/button_filter"
        android:layout_width="40dp"
        android:layout_height="32dp"
        android:layout_marginTop="150dp"
        android:background="@android:color/transparent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toEndOf="@id/admin_profile_search"
        app:layout_constraintTop_toTopOf="parent"
        android:contentDescription="Filter icon that opens filter selection for events"
        android:src="@drawable/filter" />


    <ListView
        android:id="@+id/profiles_list_view"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:divider="@android:color/transparent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        android:layout_marginTop="10dp"
        app:layout_constraintTop_toBottomOf="@id/admin_profile_search"
        app:layout_constraintBottom_toBottomOf="parent"
        android:dividerHeight="8dp"
        android:paddingHorizontal="16dp" />

</androidx.constraintlayout.widget.ConstraintLayout>